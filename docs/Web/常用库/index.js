const lodash = require('./Lodash');
const katex = require('./KaTeX');
const moment = require('./moment');
const dayjs = require('./dayjs');

module.exports = {
  title: '常用库',
  children: [lodash, katex, moment, dayjs]
};
